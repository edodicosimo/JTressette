@startuml tressette{
    package model{
        class Card{
            - rank : Rank
            - suit : Suit
        }
        class Deck{
            - cards : List<Card>
            + shuffle() : void
            + draw() : Card
        }
        enum Suit{

        }
        enum Rank{
            
        }
        interface GameRules{

        }
        class TresetteRules implements GameRules{

        }
        class Game{
            - rules : GameRules
            - numberOfPlayers : int
            - players : List<Player>
            - deck : Deck
            - table : Table
            + dealCard(deck: Deck, player: Player) : void
            + askPlayerForCard(player : Player) : Card
            + addCardToTable(table : Table, card : Card) : void
            + solveTable(table : Table)
        }

        class Hand{
            - cards : List<Card>
        }
        class Table{
            - cards : Map<Player,Card>
            + addCard(card : Card)
            + clear() : void
        }
        abstract Player{
            - hand : Hand
            - pile : List<Card>
            + receiveCard(card : Card) : void
            + playCard() : Card
        }
        class HumanPlayer extends Player{
            
        }
        class AIPlayer extends Player{

        }
        Game --* Deck
        Game --* Table
        Game --* "2..4" Player
        Player --* Hand
        Deck --o Card
        Table --o Card
        Hand --o Card
        Card --> Suit
        Card --> Rank
    }
    package controller{

    }
    package view{

    }
}